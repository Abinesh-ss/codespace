This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
app/
  navigate/
    page.tsx
  globals.css
  layout.tsx
  page.tsx
components/
  CameraScanner.tsx
  Compass.tsx
  FloorDetector.tsx
  LiveLocation.tsx
  MapOverlay.tsx
  NavigationSteps (1).tsx
  NavigationSteps.tsx
  QRAnchorScanner.tsx
  TurnArrow.tsx
lib/
  indoorPositioning.ts
  map_data.json
  types.ts
public/
  manifest.json
next-env.d.ts
next.config.mjs
package.json
postcss.config.js
README.md
tailwind.config.js
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="app/navigate/page.tsx">
"use client";
import { useState, useEffect } from "react";
import QRAnchorScanner from "@/components/QRAnchorScanner";
import MapOverlay from "@/components/MapOverlay";
import NavigationSteps from "@/components/NavigationSteps";
import { Compass, Map as MapIcon, Navigation } from "lucide-react";

export default function NavigatePage() {
  const [floor, setFloor] = useState(0);
  const [pos, setPos] = useState({ x: 50, y: 50 });
  const [currentLocation, setCurrentLocation] = useState("Scan a QR to Begin");

  // Industrial UI state for switching between Camera view and Map view
  const [viewMode, setViewMode] = useState<"camera" | "map">("camera");

  const handleQRDetect = (data: any) => {
    if (data.x !== undefined && data.y !== undefined) {
      setFloor(data.floor || 0);
      setPos({ x: data.x, y: data.y });
      setCurrentLocation(data.label || `Zone ${data.id || 'Alpha'}`);
      
      // Haptic feedback (vibration) when QR is detected (standard for industrial apps)
      if (typeof window !== "undefined" && window.navigator.vibrate) {
        window.navigator.vibrate(100);
      }
    }
  };

  return (
    <main className="fixed inset-0 bg-slate-950 flex flex-col font-sans text-slate-50">
      {/* --- HEADER --- */}
      <header className="p-4 bg-slate-900/80 backdrop-blur-md border-b border-slate-800 flex justify-between items-center">
        <div>
          <h1 className="text-lg font-bold tracking-tight">HospiNav <span className="text-blue-500">PRO</span></h1>
          <p className="text-xs text-slate-400 flex items-center gap-1">
            <Compass size={12} className="text-blue-400" /> {currentLocation}
          </p>
        </div>
        <div className="bg-blue-600 px-3 py-1 rounded-full text-xs font-mono">
          FL {floor}
        </div>
      </header>

      {/* --- MAIN VIEWPORT --- */}
      <div className="relative flex-1 overflow-hidden">
        {viewMode === "camera" ? (
          <div className="w-full h-full">
             <QRAnchorScanner onDetect={handleQRDetect} />
             {/* Scanning Frame Overlay */}
             <div className="absolute inset-0 border-[40px] border-slate-950/40 pointer-events-none flex items-center justify-center">
                <div className="w-64 h-64 border-2 border-blue-400 rounded-2xl shadow-[0_0_20px_rgba(59,130,246,0.5)]"></div>
             </div>
          </div>
        ) : (
          <div className="w-full h-full p-4 bg-slate-800">
            <MapOverlay floor={floor} x={pos.x} y={pos.y} />
          </div>
        )}

        {/* Floating View Switcher */}
        <div className="absolute top-4 right-4 flex flex-col gap-2">
          <button 
            onClick={() => setViewMode(viewMode === "camera" ? "map" : "camera")}
            className="p-3 bg-white text-slate-900 rounded-full shadow-xl hover:scale-110 transition-transform"
          >
            {viewMode === "camera" ? <MapIcon size={24} /> : <Navigation size={24} />}
          </button>
        </div>
      </div>

      {/* --- BOTTOM DRAWER (Navigation Instructions) --- */}
      <section className="bg-white text-slate-900 rounded-t-[32px] p-6 shadow-2xl">
        <div className="w-12 h-1.5 bg-slate-200 rounded-full mx-auto mb-6" />
        <NavigationSteps />
      </section>
    </main>
  );
}
</file>

<file path="app/globals.css">
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --foreground-rgb: 15, 23, 42;
  --background-start-rgb: 248, 250, 252;
  --background-end-rgb: 255, 255, 255;
}

body {
  color: rgb(var(--foreground-rgb));
  background: white;
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}
</file>

<file path="app/layout.tsx">
import type { Metadata, Viewport } from "next";
import "./globals.css"; // Ensure your Tailwind styles are loaded

export const metadata: Metadata = {
  title: "HospiNav Pro",
  description: "Advanced Hospital Indoor Navigation",
  manifest: "/manifest.json",
  appleWebApp: {
    capable: true,
    statusBarStyle: "black-translucent",
    title: "HospiNav Pro",
  },
  formatDetection: {
    telephone: false,
  },
};

// This prevents the user from "pinch-zooming," which is standard for PWAs
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: "#1d4ed8", // Matches your blue-700 theme
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="antialiased">
      <body className="bg-slate-50 text-slate-900 selection:bg-blue-100">
        {children}
      </body>
    </html>
  );
}
</file>

<file path="app/page.tsx">
"use client";
import { useRouter } from "next/navigation";
import { MapPin, ScanLine, ShieldCheck, ChevronRight } from "lucide-react";

export default function Home() {
  const router = useRouter();

  return (
    <main className="min-h-screen bg-slate-50 flex flex-col items-center">
      {/* 1. TOP HERO SECTION: Using a rounded-bottom container for visual depth */}
      <section className="w-full bg-blue-700 pt-16 pb-24 px-6 rounded-b-[48px] shadow-2xl text-center">
        <div className="bg-white/20 w-16 h-16 rounded-2xl flex items-center justify-center mx-auto mb-6 backdrop-blur-lg border border-white/30">
          <MapPin size={32} className="text-white" />
        </div>
        <h1 className="text-3xl font-extrabold text-white tracking-tight mb-2">
          HospiNav <span className="font-light opacity-80">Pro</span>
        </h1>
        <p className="text-blue-100 text-sm max-w-[280px] mx-auto leading-relaxed">
          Navigate the medical center with ease using our live AR-positioning system.
        </p>
      </section>

      {/* 2. FLOATING ACTION CARD: Positioned halfway between sections */}
      <div className="w-full max-w-md px-6 -mt-12">
        <button 
          onClick={() => router.push("/navigate")}
          className="w-full bg-white p-6 rounded-3xl shadow-xl flex items-center gap-5 hover:bg-slate-50 active:scale-[0.98] transition-all border border-slate-100"
        >
          <div className="bg-blue-600 p-4 rounded-2xl shadow-lg shadow-blue-200">
            <ScanLine size={28} className="text-white" />
          </div>
          <div className="text-left">
            <h3 className="text-slate-900 font-bold text-lg leading-tight">Start Navigation</h3>
            <p className="text-slate-500 text-sm font-medium">Scan a QR code near you</p>
          </div>
          <ChevronRight className="ml-auto text-slate-300" size={24} />
        </button>
      </div>

      {/* 3. "HOW TO USE" SECTION: Using a vertical step-indicator layout */}
      <section className="w-full max-w-md p-8 mt-6">
        <div className="flex justify-between items-end mb-8">
          <h4 className="text-slate-400 font-bold text-[11px] uppercase tracking-[0.2em]">How to use</h4>
          <div className="h-px bg-slate-200 flex-1 ml-4 mb-1.5 opacity-50"></div>
        </div>

        <div className="space-y-8">
          {/* Step 1 */}
          <div className="flex gap-5">
            <div className="flex flex-col items-center">
              <div className="w-8 h-8 rounded-full bg-blue-50 text-blue-600 text-xs flex items-center justify-center font-bold border border-blue-100 ring-4 ring-white">1</div>
              <div className="w-px h-full bg-slate-200 my-2"></div>
            </div>
            <div className="pb-2">
              <p className="text-slate-800 font-bold text-sm mb-1">Find a Navigator QR</p>
              <p className="text-slate-500 text-xs leading-relaxed">Located at every entrance, hallway intersection, and elevator.</p>
            </div>
          </div>

          {/* Step 2 */}
          <div className="flex gap-5">
            <div className="flex flex-col items-center">
              <div className="w-8 h-8 rounded-full bg-blue-50 text-blue-600 text-xs flex items-center justify-center font-bold border border-blue-100 ring-4 ring-white">2</div>
              <div className="w-px h-full bg-slate-200 my-2"></div>
            </div>
            <div className="pb-2">
              <p className="text-slate-800 font-bold text-sm mb-1">Allow Camera Access</p>
              <p className="text-slate-500 text-xs leading-relaxed">We use the camera only to detect your live indoor position via stickers.</p>
            </div>
          </div>

          {/* Step 3 */}
          <div className="flex gap-5">
            <div className="flex flex-col items-center">
              <div className="w-8 h-8 rounded-full bg-blue-50 text-blue-600 text-xs flex items-center justify-center font-bold border border-blue-100 ring-4 ring-white">3</div>
            </div>
            <div>
              <p className="text-slate-800 font-bold text-sm mb-1">Follow the Blue Line</p>
              <p className="text-slate-500 text-xs leading-relaxed">Real-time arrows and paths will guide you to your room or department.</p>
            </div>
          </div>
        </div>
      </section>

      {/* 4. FOOTER: Using a subtle layout for trust markers */}
      <footer className="mt-auto p-10 flex flex-col items-center gap-3">
        <div className="flex items-center gap-2 px-3 py-1 bg-slate-100 rounded-full text-slate-400">
          <ShieldCheck size={14} className="text-emerald-500" />
          <span className="text-[10px] font-bold tracking-widest uppercase">Encrypted Connection</span>
        </div>
        <p className="text-[10px] text-slate-300 font-medium">© 2026 HospiNav Pro v2.4</p>
      </footer>
    </main>
  );
}
</file>

<file path="components/CameraScanner.tsx">
"use client";
import { useEffect, useRef } from "react";

export default function CameraScanner() {
  const ref = useRef<HTMLVideoElement>(null);

  useEffect(() => {
    navigator.mediaDevices.getUserMedia({ video: { facingMode: "environment" } })
      .then(stream => {
        if (ref.current) {
          ref.current.srcObject = stream;
          ref.current.play();
        }
      });
  }, []);

  return <video ref={ref} style={{ width: "100%" }} />;
}
</file>

<file path="components/Compass.tsx">
"use client";
import { useEffect, useState } from "react";

export default function Compass() {
  const [angle, setAngle] = useState<number | null>(null);

  useEffect(() => {
    const handler = (e: DeviceOrientationEvent) => {
      if (e.alpha !== null) setAngle(e.alpha);
    };
    window.addEventListener("deviceorientation", handler, true);
    return () => window.removeEventListener("deviceorientation", handler);
  }, []);

  return <p>Heading: {angle?.toFixed(1)}°</p>;
}
</file>

<file path="components/FloorDetector.tsx">
"use client";
import { useEffect } from "react";

export default function FloorDetector({ onFloor }: { onFloor: (f: number) => void }) {
  useEffect(() => {
    const saved = localStorage.getItem("floor");
    if (saved) onFloor(Number(saved));
  }, []);

  return null;
}
</file>

<file path="components/LiveLocation.tsx">
"use client";
import { useEffect, useState } from "react";

export default function LiveLocation() {
  const [pos, setPos] = useState<any>(null);

  useEffect(() => {
    const id = navigator.geolocation.watchPosition(
      p => setPos(p.coords),
      e => console.error(e),
      { enableHighAccuracy: true }
    );
    return () => navigator.geolocation.clearWatch(id);
  }, []);

  if (!pos) return <p>Locating...</p>;
  return <p>Lat: {pos.latitude}, Lng: {pos.longitude}</p>;
}
</file>

<file path="components/MapOverlay.tsx">
"use client";

export default function MapOverlay({ floor, x, y }: any) {
  return (
    <div className="relative w-full aspect-square bg-slate-100 rounded-xl border-2 border-dashed border-slate-300 overflow-hidden">
      {/* Fallback Grid Pattern when image is missing */}
      <div className="absolute inset-0 opacity-10" 
           style={{ backgroundImage: 'radial-gradient(#000 1px, transparent 1px)', size: '20px 20px' }} />
      
      {/* The Map Image (only shows if file exists) */}
      <img 
        src={`/maps/floor-${floor}.png`} 
        alt={`Floor ${floor}`}
        className="w-full h-full object-contain"
        onError={(e) => (e.currentTarget.style.display = "none")} 
      />

      {/* The User's Live Position (The Red Dot) */}
      <div 
        style={{
          position: "absolute",
          left: `${x}%`,
          top: `${y}%`,
          width: 16,
          height: 16,
          background: "red",
          boxShadow: "0 0 10px rgba(255,0,0,0.5)",
          borderRadius: "50%",
          transform: "translate(-50%, -50%)",
          transition: "all 0.5s ease"
        }} 
      />
      
      <div className="absolute bottom-2 left-2 bg-white/80 px-2 py-1 rounded text-xs">
        Floor: {floor} | X: {x}% Y: {y}%
      </div>
    </div>
  );
}
</file>

<file path="components/NavigationSteps (1).tsx">
"use client";
import { useEffect, useState } from "react";

export default function NavigationSteps() {
  const [steps, setSteps] = useState<string[]>([]);

  useEffect(() => {
    fetch(process.env.NEXT_PUBLIC_API_BASE_URL + "/navigation/demo")
      .then(res => res.json())
      .then(data => setSteps(data.steps || []));
  }, []);

  return (
    <ol>
      {steps.map((s, i) => <li key={i}>{s}</li>)}
    </ol>
  );
}
</file>

<file path="components/NavigationSteps.tsx">
"use client";
import { useEffect, useState } from "react";
import TurnArrow from "./TurnArrow";

export default function NavigationSteps() {
  const [steps, setSteps] = useState<any[]>([]);

  useEffect(() => {
    fetch(process.env.NEXT_PUBLIC_API_BASE_URL + "/navigation/route")
      .then(res => res.json())
      .then(data => setSteps(data.steps || []));
  }, []);

  return (
    <div>
      {steps.map((s, i) => (
        <div key={i}>
          <p>{s.instruction}</p>
          <TurnArrow direction={s.direction} />
        </div>
      ))}
    </div>
  );
}
</file>

<file path="components/QRAnchorScanner.tsx">
"use client";
import { useEffect, useRef } from "react";

export default function QRAnchorScanner({ onDetect }: { onDetect: (data: any) => void }) {
  // 1. Explicitly type the ref as an HTMLVideoElement
  const videoRef = useRef<HTMLVideoElement>(null);

  useEffect(() => {
    let activeStream: MediaStream | null = null;

    const startCamera = async () => {
      try {
        const stream = await navigator.mediaDevices.getUserMedia({ 
          video: { facingMode: "environment" } 
        });
        
        activeStream = stream;

        // 2. Use a local variable to ensure the ref hasn't changed
        const currentVideo = videoRef.current;
        if (currentVideo) {
          currentVideo.srcObject = stream;
          // 3. playsInline is mandatory for iOS Safari support
          currentVideo.setAttribute("playsinline", "true");
          currentVideo.play();
        }
      } catch (err) {
        console.error("Camera access denied or not available:", err);
      }
    };

    startCamera();

    // 4. CLEANUP: This stops the camera lens from staying "ON" (green light)
    // when the user navigates away from the page.
    return () => {
      if (activeStream) {
        activeStream.getTracks().forEach(track => track.stop());
      }
    };
  }, []);

  return (
    <div className="relative w-full h-full bg-black rounded-2xl overflow-hidden shadow-2xl border-2 border-slate-800">
      <video 
        ref={videoRef} 
        className="w-full h-full object-cover"
        muted 
      />
      {/* Visual scanning guide */}
      <div className="absolute inset-0 border-[3px] border-blue-500/30 m-12 rounded-xl pointer-events-none">
        <div className="absolute top-0 left-0 w-8 h-8 border-t-4 border-l-4 border-blue-500 rounded-tl-lg" />
        <div className="absolute top-0 right-0 w-8 h-8 border-t-4 border-r-4 border-blue-500 rounded-tr-lg" />
        <div className="absolute bottom-0 left-0 w-8 h-8 border-b-4 border-l-4 border-blue-500 rounded-bl-lg" />
        <div className="absolute bottom-0 right-0 w-8 h-8 border-b-4 border-r-4 border-blue-500 rounded-br-lg" />
      </div>
    </div>
  );
}
</file>

<file path="components/TurnArrow.tsx">
"use client";
export default function TurnArrow({ direction }: { direction: number }) {
  return (
    <div style={{
      width: 60,
      height: 60,
      transform: `rotate(${direction}deg)`,
      transition: "transform 0.3s"
    }}>
      ⬆️
    </div>
  );
}
</file>

<file path="lib/indoorPositioning.ts">
import { Anchor } from "./types";

export function calculatePosition(anchor: Anchor) {
  return {
    floor: anchor.floor,
    x: anchor.x,
    y: anchor.y
  };
}
</file>

<file path="lib/map_data.json">
{
  "anchors": [
    { "id": "QR_ENTRANCE", "floor": 0, "x": 10, "y": 20, "label": "Main Entrance" },
    { "id": "QR_ELEVATOR_A", "floor": 1, "x": 45, "y": 50, "label": "Floor 1 Elevators" },
    { "id": "QR_PHARMACY", "floor": 0, "x": 80, "y": 15, "label": "Pharmacy" }
  ],
  "paths": [
    { "from": "QR_ENTRANCE", "to": "QR_PHARMACY", "distance_meters": 35 }
  ]
}
</file>

<file path="lib/types.ts">
export interface Anchor {
  id: string;
  floor: number;
  x: number; // Represents percentage (0-100)
  y: number; // Represents percentage (0-100)
  label?: string;
}

export interface RouteStep {
  instruction: string;
  direction: number; // Degrees: 0 for straight, -90 for left, 90 for right
}
</file>

<file path="public/manifest.json">
{
  "name": "Vazhikatti: Hospital Navigation",
  "short_name": "HospiNav",
  "description": "Advanced indoor navigation using QR anchors and floor detection.",
  "start_url": "/",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#ffffff",
  "theme_color": "#0f172a",
  "icons": [
    {
      "src": "https://cdn-icons-png.flaticon.com/512/854/854878.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}
</file>

<file path="next-env.d.ts">
/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/building-your-application/configuring/typescript for more information.
</file>

<file path="next.config.mjs">
/** @type {import('next').Next.js} */
const nextConfig = {
  reactStrictMode: true,
  // Required for camera access on some browsers and deployment environments
  images: {
    unoptimized: true, 
  },
  // Allows the app to connect to your backend API without CORS issues during dev
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: `${process.env.NEXT_PUBLIC_API_BASE_URL}/:path*`,
      },
    ];
  },
};

export default nextConfig;
</file>

<file path="package.json">
{
  "name": "Vazhikatti",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "jsqr": "^1.4.0",
    "lucide-react": "^0.294.0",
    "next": "14.x",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.4.23",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "typescript": "^5"
  }
}
</file>

<file path="postcss.config.js">
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    'autoprefixer': {},
  },
}
</file>

<file path="README.md">
# HospiNav Pro – Standard Indoor Navigation PWA

## Features
- QR anchor-based indoor positioning
- Automatic floor detection
- Map overlay with live position
- Turn-by-turn arrow rotation

## Setup
```bash
npm install
npm run dev
```

## Environment
Create `.env.local`:
```
NEXT_PUBLIC_API_BASE_URL=https://api.hospinav.pro
```

## HTTPS
```bash
npx cloudflared tunnel --url http://localhost:3000
```

## Backend Expectation
```
GET /navigation/route
{
  "steps": [
    { "instruction": "Go straight", "direction": 0 },
    { "instruction": "Turn left", "direction": -90 }
  ]
}
```

## Notes
- Replace QR decoder with `jsqr` or `zxing`
- Add real floor maps in `/public/maps/`
</file>

<file path="tailwind.config.js">
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./lib/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    },
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": false,
    "noEmit": true,
    "incremental": true,
    "module": "esnext",
    "esModuleInterop": true,
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": [
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": [
    "node_modules"
  ]
}
</file>

</files>
